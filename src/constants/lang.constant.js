"use strict";
/*
  |--------------------------------------------------------------------------
  | Language constants
  |--------------------------------------------------------------------------
*/
const langConstants = Object.freeze({
  locale: ["en", "ar"],
  default_locale: "en",
});

module.exports = { langConstants };
